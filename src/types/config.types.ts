export interface IConfig {
  siteTitle: string;
  siteDescription: string;
  siteUrl: string;
  siteType: string;
  siteImage: string;
  siteKeywords: string;
  siteVersion: string;
  siteYear: number;
}
